create table register
(
    name varchar(20);
    email varchar(20) primary key,
    mobileNo int,
    city varchar(20),
    state varchar(20),
    dob DATE,
    qualification varchar(20),
    year_of_completion varchar(20)
);